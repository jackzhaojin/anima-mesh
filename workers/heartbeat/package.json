{
  "name": "@anima-mesh/heartbeat-worker",
  "version": "0.1.0",
  "private": true,
  "description": "AnimaMesh cloud heartbeat: one Worker + one Durable Object alarm over a GitHub-hosted brain. Deploy config lives in the instance repo (cloud/); wrangler.example.jsonc here is a template.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260620.0",
    "@types/node": "^26.1.1",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
